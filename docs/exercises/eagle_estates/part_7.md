# Eagle Estates - Part 7

Update the `eagle.property` model to include the following properties:

- Records of the model should use the `name` field as the display name/label.
- Records should be ordered by the construction date in descending order. In case two records have the same construction
  dates, then sort by the ID in ascending order.
